# Define a nested tuple
nested_tuple = ((1, 2, 3), (4, 5, 6), (7, 8, 9))

# Access an element from the nested tuple (e.g., the element at row 1, column 2)
element = nested_tuple[1][2]

# Print the accessed element
print("Accessed element:", element)
